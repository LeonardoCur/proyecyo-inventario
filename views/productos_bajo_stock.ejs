<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Productos Bajo el Nivel de Stock</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/resources/css/style.css">
</head>
<body>
    <%- include('partials/header') %>
    <main>
        <div class="container">
            <h1>Productos Bajo el Nivel de Stock</h1>

            <!-- Formulario para ingresar un stock mínimo personalizado -->
                <form class="form-stock" action="/productos/bajo-stock" method="GET">
                    <div class="mb-3">
                        <label for="stock_min" class="form-label">Selecciona el nivel de stock mínimo que deseas filtrar:</label>
                        <input type="number" id="stock_min" name="stock_min" class="form-control" placeholder="Ej: 10" required>
                    </div>
                        <div class="boton_stock">

                            <button type="submit" class= "btn-primary stock-button">Filtrar</button>
                        </div>

               
                </form>


            <% if (productos.length > 0) { %>
        
                    <h2 class="resultados" >Resultados</h2>
                   
        
                <ul>
                    <% productos.forEach(producto => { %>
                        <li>
                            <span><%= producto.nombre %> - Marca: <%= producto.marca %> - Stock: <%= producto.stock %></span>
                        </li>
                    <% }) %>
                </ul>
            <% } else { %>
                <p>No hay productos por debajo del nivel de stock mínimo seleccionado.</p>
            <% } %>
        </div>
    </main>

    <%- include('partials/footer') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
